import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hello.react.android.tabs.example',
  appName: 'hello-react-android-tabs-example',
  webDir: 'dist'
};

export default config;
